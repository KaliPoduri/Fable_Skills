# m0-canary (THROWAWAY)

Exists only to run the M0 feasibility spike (PLAN.md §8, M0). Delete the
whole `m0/` kit once the M0 gate closes. Full instructions:
`m0/M0-CHECKLIST.md`.

Not part of the Fable Skills library (it lives under `m0/`, not `skills/`,
and is not scanned by the validators). Pure markdown, no scripts — safe to
copy into a disposable test repo. Do not install it anywhere real.
